spring:
  application:
    name: test-springboot-3

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

info:
  app:
    name: test-springboot-3
    cloud: aws
    cost-center: MC-1234
