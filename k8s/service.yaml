apiVersion: v1
kind: Service
metadata:
  name: test-springboot-3
  namespace: sandbox-test-springboot-3
  labels:
    app: test-springboot-3
    tenant: mastercard
spec:
  type: ClusterIP
  selector:
    app: test-springboot-3
  ports:
    - name: http
      port: 80
      targetPort: 8080
